import { BigNumber } from '@ethersproject/bignumber';
import { Board } from '../board';
export default function getOptionPriceFee(board: Board, pricePerOption: BigNumber, size: BigNumber): BigNumber;
