import { BigNumber } from '@ethersproject/bignumber';
export default function getBreakEvenPrice(isCall: boolean, strikePrice: BigNumber, optionPrice: BigNumber, isBaseCollateral?: boolean): BigNumber;
